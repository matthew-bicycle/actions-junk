name: "📚 Deploy service: API"
run-name: Deploy API to ${{ inputs.envName }}

on:
  workflow_call:
    inputs:
      envName:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      envName:
        required: true
        type: string

jobs:
  DeployApiService:
    uses: ./.github/workflows/CloudRunServiceDeployment.yml
    secrets: inherit
    with:
      serviceName: api-${{ inputs.envName }}
      envName: ${{ inputs.envName }}
  DeployApiInfra:
    uses: ./.github/workflows/InfraStackDeployment.yml
    secrets: inherit
    with:
      envName: ${{ inputs.envName }}
      serviceName: api-${{ inputs.envName }}
