name: On Push

on:
  push:

jobs:
  Build:
    uses: ./.github/workflows/Build.yml
  DevDeploy:
    if: ${{ github.ref == 'refs/heads/main' }} # FIXME: verify ref name
    needs: [Build]
    uses: ./.github/workflows/env-deploy-dev.yml
