name: _Cloud Run Deployment
run-name: "Cloud Run Deployment | ${{ inputs.serviceName }} in ${{ inputs.envName }}"

on:
  workflow_call:
    inputs:
      serviceName:
        type: string
        required: true
      envName:
        type: string
        required: true

jobs:
  CloudRunServiceDeployment:
    name: Deploy ${{ inputs.serviceName }}→${{ inputs.envName }} (Cloud Run Service)
    environment: ${{ inputs.envName }}
    concurrency:
      group: CloudRunServiceDeployment-${{ inputs.serviceName }}-${{ inputs.envName }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deploy Run Service: ${{ inputs.serviceName }}"
