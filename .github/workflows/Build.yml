name: Build

on:
  workflow_call:

permissions:
  contents: read

jobs:
  Build:
    concurrency:
      group: Build-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building code"
      - run: echo "Pushing image"
